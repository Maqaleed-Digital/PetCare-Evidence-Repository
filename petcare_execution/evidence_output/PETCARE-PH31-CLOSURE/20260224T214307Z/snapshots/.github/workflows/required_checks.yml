name: required-checks

on:
  pull_request:

jobs:
  required:
    runs-on: ubuntu-latest
    steps:
      - name: Required checks present
        shell: bash
        run: |
          set -euo pipefail
          echo "REQUIRED_CHECKS=PASS"
