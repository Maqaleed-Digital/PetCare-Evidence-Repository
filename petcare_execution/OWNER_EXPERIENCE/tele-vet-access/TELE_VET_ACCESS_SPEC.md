TELE VET ACCESS SPEC

Purpose:
Define deterministic owner access into tele-vet session context.

Rules:
- session access linked to valid appointment and owner identity
- access state must return explicit grant or deny outcome
- consultation access packet may expose allowed pre-session context only
- clinical authority remains with veterinarian
