CATALOG NORMALIZATION SPEC

Purpose:
Define the deterministic catalog ingestion and normalization path.

Rules:
- catalog items require normalized identifiers
- invalid records may not pass silently
- normalization output must be reproducible
- source partner reference must be preserved
