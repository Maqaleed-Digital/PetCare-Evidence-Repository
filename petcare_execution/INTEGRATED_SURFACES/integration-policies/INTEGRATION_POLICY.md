INTEGRATION POLICY

Rules:
- surfaces must use runtime domain APIs
- surfaces cannot bypass audit logging
- surfaces cannot access patient data without consent verification
- cross-domain data flows must emit audit events
