RELEASE BUNDLE CONTRACT

Required bundle contents:
- manifest
- sha256 digest
- file listing
- validation log
- audit sample log
- release gate result

Rules:
- release bundle must be deterministic
- missing required artifact blocks promotion
- bundle id must remain linked to release candidate id
