ROLLBACK PLAN

Purpose:
Define deterministic rollback readiness for deployment units.

Rules:
- rollback path must be defined before prod promotion
- rollback trigger conditions must be explicit
- rollback decision remains human-authorized
- rollback action must remain auditable
