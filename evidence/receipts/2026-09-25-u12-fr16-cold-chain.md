# U12 · FR-16 temperature-controlled delivery tracking — AC-FR-16-01/04 closed, 2026-09-25

```
PROGRAMME=MVC-BUILD-RUNNER-001 v1.1   UNIT=U12   BASE_MAIN=1348f7164f75893ca1ec769d32286f74835e183d (U11 merged, PR #56)
BRANCH=build/u12-fr16-delivery
SELECTION (pass 1): FR-16 = 2 dependency criteria, ABSENT; next by id after FR-06.
CRITERIA_CLOSED=[AC-FR-16-01, AC-FR-16-04]
GAPS_REMAINING={AC-FR-16-02: [DEPENDENCY, SERVED_APP_E2E], AC-FR-16-03: [DEPENDENCY, SERVED_APP_E2E] — EXTERNAL:LOGISTICS_PARTNER}
FR-16: ABSENT / EVIDENCE_INCOMPLETE  ->  REACHABLE_TESTED / EVIDENCE_INCOMPLETE (exclusively EXTERNAL-gated)
```

## Build
- Migration 0045: storage range on `product_registration` (BRD P393; registration fact, CHECK both-or-neither and
  min < max); `delivery` (range copied at creation), `temperature_reading`, `delivery_alert` (one per reading),
  `delivery_completion` — insert-only.
- `deliveries.py` + `PostgresDeliveryRepository`: reading and alert in ONE transaction; no reading after completion.
- Routes: `POST /api/deliveries` (tenant staff; recipient an owner of the tenant; no range from the request),
  `POST …/{id}/readings` (staff; out-of-range kept, flagged, alert raised, audited `delivery.temperature.alert`),
  `POST …/{id}/complete` (a cold-chain delivery with an empty log → 409), `GET /api/deliveries` (owner: own with log;
  staff: tenant), `GET /api/deliveries/alerts` (the pharmacy's alert list), `GET …/{id}`.
- UI `/owner/deliveries`: the owner's deliveries with the temperature log, out-of-range readings shown (Arabic RTL).

## Evidence
AC-01 {SERVED_APP_E2E x2, PERSISTENCE (PG), UI ARTEFACT} · AC-04 {AUDIT, TENANT_ISOLATION}. AC-02/03 NOT registered:
readings enter through the adapter boundary (`normalise_partner_reading`), whose refusals are tested, but the logistics
partner's interface contract is not held.

## Perturbations
```
P-AC01-COMPLETE-NOLOG      cold-chain completes with empty log      -> AC-01 test FAILS            ARMED
P-AC01-OWNER-BLIND         owner cannot list own deliveries         -> AC-01 test FAILS            ARMED
P-AC01-NO-RANGE            range not taken from registration        -> AC-01 test FAILS            ARMED
P-AC01-UI-NOLOG            page renders no readings                 -> vitest FAILS                ARMED
P-AC01-DB-AFTER-COMPLETE   PG accepts a reading after completion    -> PG test FAILS               ARMED
P-AC04-NOALERT             no alert on out-of-range                 -> AC-04 test FAILS            ARMED
P-AC04-UNAUDITED           alert not audited                        -> AC-04 test FAILS            ARMED
P-AC04-TENANT              alert list not tenant-scoped             -> AC-04 test FAILS            ARMED
P-AC02-NO-TIMESTAMP        explicit missing-timestamp check removed -> VACUOUS (first attempt): the malformed-value
                           parse also refuses None, so the mutation modelled no reachable defect. REDESIGNED as:
P-AC02-DEFAULTED-TIMESTAMP missing timestamp silently defaulted     -> refusal test FAILS          ARMED
FINAL SET: PERTURBATIONS=9 ARMED=9 VACUOUS=0
```
