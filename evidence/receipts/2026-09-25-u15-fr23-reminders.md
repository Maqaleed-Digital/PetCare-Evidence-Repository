# U15 · FR-23 vaccination and treatment reminders — AC-FR-23-01/02 closed, 2026-09-25

```
PROGRAMME=MVC-BUILD-RUNNER-001 v1.1   UNIT=U15   BASE_MAIN=d5b4a5b9165d4e328c1d93a9fbd654f809be02b3 (U14 merged, PR #59)
BRANCH=build/u15-fr23
SELECTION (pass 1): FR-23 = 2 dependency criteria, ABSENT; next by id after FR-20.
CRITERIA_CLOSED=[AC-FR-23-01, AC-FR-23-02]
GAPS_REMAINING={AC-FR-23-03: [DEPENDENCY, SERVED_APP_E2E], AC-FR-23-04: [DEPENDENCY, SERVED_APP_E2E] — EXTERNAL:SMS_GATEWAY}
FR-23: ABSENT / EVIDENCE_INCOMPLETE  ->  REACHABLE_TESTED / EVIDENCE_INCOMPLETE (exclusively EXTERNAL-gated)
```

## Build
- Migration 0048: `pet_care_due` (FK pet_profile), `pet_care_completion`, `care_reminder` UNIQUE(due_id, kind) — a
  repeated scheduler run is a no-op. Insert-only.
- `reminders.py`: ratified defaults REMIND_7D (7 days before due) and REMIND_24H (24 hours before due, only while
  outstanding); no suppression path exists. Rendering in Arabic (primary) or English.
- Routes: `POST /api/pets/{id}/care-due` (veterinarian, live authority), `…/{due_id}/complete`, `GET …/care-due`,
  `POST /api/reminders/run` (tenant/platform admin; the session tenant only; the owner of THIS tenant's pet; language
  from the owner's stored preference; each send audited `reminder.sent`), `GET /api/me/reminders`.
- UI `/owner/reminders` (Arabic RTL).

## Evidence
AC-01 {SERVED_APP_E2E, PERSISTENCE (PG), TENANT_ISOLATION, UI ARTEFACT} · AC-02 {AUDIT, ARABIC_RTL ARTEFACT}.
The 24-hour reminder is proven by moving the served app's clock past the threshold; the completed item receives none.
AC-03/04 NOT exercised (IN_APP delivery only; no SMS adapter).

## Perturbations
```
P-AC01-NO-7D              7-day reminder never sent                          -> AC-01 test FAILS        ARMED
P-AC01-24H-EVEN-IF-DONE   24-hour reminder ignores completion                -> AC-01 test FAILS        ARMED
P-AC01-NOT-IDEMPOTENT     repository and scheduler both forget prior sends   -> AC-01 test FAILS        ARMED
P-AC01-NO-TENANT-SCOPE    dues unscoped AND pet looked up in its own tenant  -> isolation test FAILS    ARMED
P-AC01-UI-HIDDEN          page renders no reminders                          -> vitest FAILS            ARMED
P-AC02-ALWAYS-EN          language ignored                                   -> AC-02 test FAILS        ARMED
P-AC02-UNAUDITED          send not audited                                   -> AC-02 test FAILS        ARMED
P-AC02-UI-LTR             Arabic reminder rendered LTR                       -> vitest FAILS            ARMED
P-AC01-DB-NO-UNIQUE       UNIQUE(due, kind) + ON CONFLICT removed            -> PG test FAILS           ARMED
PERTURBATIONS=9 ARMED=9 VACUOUS=0
```
The two-layer perturbations (NOT-IDEMPOTENT, NO-TENANT-SCOPE, DB-NO-UNIQUE) mutate every layer that holds the rule,
after U12/U14 showed single-layer mutations of defended rules are vacuous.

## Findings
- Production scheduling (a timer calling the run for each tenant) is a PRODUCTION act, outside the programme.
- Tenant/user configuration that ADDS reminders is not built; the ratified text permits but does not require it.
