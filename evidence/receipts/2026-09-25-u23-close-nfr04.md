# U23 · closing unit — NFR-04 evidenced; runner state at stop, 2026-09-25

```
PROGRAMME=MVC-BUILD-RUNNER-001 v1.1   UNIT=U23   BASE_MAIN=c2552636fda5551a7d3068fa3e97974659be1c19 (U22 merged, PR #67)
BRANCH=build/u23-close-nfr04
CHANGE: evidence.json NFR-04 <- petcare_api/tests/test_inventory_postgres.py::test_inventory_check_is_sub_second_on_production_sized_data
        (NFR_EVIDENCE). The ratified NFR-04 evidence definition is "timed inventory checks on production-sized data",
        environment NON_PROD, dependency NONE, threshold sub-second — exactly what the U8 test measures (worst of 20
        runs < 1 s over 300,000 movements; per-location 22 ms, per-product 1 ms locally). No code changed.
NFR-04: EVIDENCE_INCOMPLETE -> EVIDENCED. No FR state changed.
```

## Why the runner stops here
Every Phase-1 High FR is ACCEPTED (FR-02, FR-09) or REACHABLE_TESTED with all remaining criterion gaps held by named
COUNSEL / EXTERNAL / PRODUCTION dependencies — EXCEPT three criteria whose remaining gap is a Sponsor product decision:
- AC-FR-01-03 — SQ-1 (how tenantless pre-session account events are chained); everything else in it is built (U20).
- AC-FR-06-01/-02 — SQ-2 (how video is accepted while REG-02 keeps remote consultation fail-closed); the capability is
  built behind the gate (U22).
There is no further internally buildable criterion work. The terminal condition (only COUNSEL/EXTERNAL/PRODUCTION
dependencies) is therefore not met; the runner stops at the Sponsor decisions — not at a failed unit.

## Internal NFR work not in the FR unit loop (for scheduling)
NFR-15 API rate limiting (served-app throttling test) and NFR-08 MFA for sensitive operations are internally buildable,
dependency NONE; NFR-01/02/03 require load/instrumented measurement in a NON_PROD acceptance environment at the stated
scale (10,000 concurrent users). NFR-05/07/10/11/12 are PRODUCTION; NFR-06 COUNSEL:EV-11; NFR-09 COUNSEL:REG-03_PDPL.
