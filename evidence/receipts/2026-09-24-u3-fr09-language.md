# U3 · FR-09 multi-language — AC-FR-09-01/02 closed; AC-FR-09-03 open, 2026-09-24

```
PROGRAMME=MVC-BUILD-RUNNER-001 v1.1   UNIT=U3   BASE_MAIN=d9c7b26e9503e336bebab030440dcd7a1244f4c0 (U2 merged)
BRANCH=build/u3-fr09-language   SELECTION: FR-09 = 0 dependency criteria, next after FR-02 (BUILT_UNWIRED)
CRITERIA_CLOSED=[AC-FR-09-01, AC-FR-09-02]
GAPS_REMAINING={AC-FR-09-03: [ARABIC_RTL, SERVED_APP_E2E] — no notification or generated document exists in the
                served app yet; closes with the FR-20 (receipt) / FR-23 (reminder) units. Not dependency-blocked.}
FR-09: BUILT_UNWIRED / EVIDENCE_INCOMPLETE  ->  REACHABLE_TESTED / EVIDENCE_INCOMPLETE
```

## Measurement first
A render of every page in Arabic mode listed Latin-only text: brand names and e-mail addresses (allowed),
`/owner/emergency` triage severity badges rendered as raw `critical/urgent/routine` (a real AC-FR-09-01 defect),
and `/admin` internal function names (platform-operator console — not customer-facing; recorded, not changed).

## Build
- **UI (AC-FR-09-01).** Emergency severity badges localised (`lib/strings.ts` severityBadge*, page renders `t(...)`).
- **Server-held preference (AC-FR-09-02 "survives a new session").** Migration `0038_fr09_user_preference.sql`
  (additive; `user_preference` → user_identity, tenant) · `petcare_api/preferences.py` (Arabic default, closed set,
  tenant-checked in-memory repo) · `PostgresPreferenceRepository` · `Persistence.preferences` · routes
  `GET/PUT /api/me/preferences/language` (session actor + tenant; PUT audited `user.preference.language.set`).
- **Web.** `LangProvider` gains `setLanguage` (no fetches in the provider); `LanguageToggle` stores the choice when
  signed in; sign-in restores a stored choice. Arabic stays the default for a new visitor.

## Tests
```
petcare_api/tests/test_language_preference.py (served_app, 5): Arabic default · survives a NEW session (fresh client,
    fresh sign-in) · per identity · unknown language 400 / unauthenticated 401 · audited as session actor
petcare_api/tests/test_language_preference_postgres.py (2): second-instance read-back; unknown language/identity refused
petcare_web/__tests__/language-fr09.test.tsx (vitest, 29): 13 customer-facing pages x {Arabic default + RTL + no
    English-only text, English + LTR} · toggle stores when signed in / not when signed out · sign-in restores
PERTURBATIONS
  P-AC01 emergency badge back to raw severity key -> '/owner/emergency: Arabic by default' FAILS   ARMED
  P-AC02 server ignores the stored language       -> test_choice_survives_a_new_session FAILS     ARMED
```

## Binding and evidence
- bindings FR-09: +4 implements, routes [GET, PUT /api/me/preferences/language], 2 test files; fitness storage
  NONE → PERSISTENT_POSTGRES, tenant/actor SESSION; `unresolved` states the AC-FR-09-03 gap and the /admin exclusion.
- evidence.json: AC-FR-09-01 {UI, ARABIC_RTL: ARTEFACT language-fr09.test.tsx} · AC-FR-09-02 {UI, ARABIC_RTL:
  ARTEFACT; SERVED_APP_E2E: TEST test_choice_survives_a_new_session}.
- served_routes.json regenerated: 38 → 40.

## Results
```
REGRESSION_LOCAL=1002 passed / 0 skipped · GOVERNANCE=277 passed · WEB_UNIT(local)=160 passed · TSC clean
M4: no FR lowered; FR-02 remains ACCEPTED
SCANNERS: SECRET_SCAN=CLEAN · ACTIVE_LITERAL_DEFAULT=0 · DIFF_CHECK=CLEAN
CI config: verify.yml PostgreSQL list += test_language_preference_postgres.py
```

## Findings
- A TypeScript error in the new vitest file (untyped `vi.fn` call tuple) was caught locally by `tsc` before commit
  and fixed; the ARTEFACT sha256 was re-registered after the edit. Any later edit to an ARTEFACT-registered test file
  requires the same re-registration (the checker holds ARTEFACT evidence by hash).
- `/admin` shows English-only internal names; it is the operator console, outside "customer-facing" (Sponsor may disagree).
