# U2 · FR-02 pet profile — AC-FR-02-01/02/03 (FR-02 reaches ACCEPTED), 2026-09-24

```
PROGRAMME=MVC-BUILD-RUNNER-001 v1.1   UNIT=U2   BASE_MAIN=2069dea2c49efb70976f1a5801d8eca6597071b8 (U1 merged)
BRANCH=build/u2-fr02-profile   SELECTION: FR-02 = 0 dependency criteria, REACHABLE_TESTED, lowest id
CRITERIA_CLOSED=[AC-FR-02-01, AC-FR-02-02, AC-FR-02-03]   (AC-FR-02-04 closed in U1)
FR-02: REACHABLE_TESTED / EVIDENCE_INCOMPLETE  ->  ACCEPTED / ACCEPTED   (first ACCEPTED FR)
```

## Build
- **Store.** `petcare_api/pets.py` (PetProfile, PetIdentification, PetMedicalRecord, Protocol, tenant-checked
  InMemory repo) · `PostgresPetProfileRepository` (postgres_repositories.py:872; tenant in every predicate) ·
  migration `0037_fr02_pet_profile.sql` (additive: pet_profile, pet_identification, pet_medical_record, all
  tenant-FK'd; identification optional with CHECKed type set) · `Persistence.pets` in both modes · PG harness
  reset order extended. The profile no longer uses the node-local UPHR JSON file on the served path.
- **Routes** (main.py): POST /api/pets (:1287), GET /api/pets (:1326), GET /api/pets/{id} (:1337, profile +
  identifications + medical history = records + prescriptions read from the prescription store), PATCH
  /api/pets/{id} (:1363), POST …/identifications (:1392), POST …/medical-records (:1418, veterinarian only).
  Actor and tenant from the session only; `tenant_id` in the body is optional and must match the session; an
  owner always creates for themself; owners see only their own pets (404, not 403); unknown fields 422; every
  write audited as the session actor.
- **UI** `petcare_web/app/owner/pets/page.tsx`: list, create (all BRD fields), profile with identification,
  medical history (lab/clinical records + prescriptions) and editable preferences; Arabic default, `dir="rtl"`.

## Tests
```
petcare_api/tests/test_pet_profile.py (served_app, 11)   AC-01 fields/owner/tenant/other-owner/unknown-field ·
    AC-02 history lab+prescription / preferences / vet-only records · AC-03 structured+optional / never free text ·
    AC-04 change audited as session actor
petcare_api/tests/test_pet_profile_postgres.py (4)       second-instance read-back of profile, identification,
    history; unregistered tenant + free-text identification refused; identity fields immutable
petcare_web/__tests__/pet-profile.test.tsx (vitest, 4)   Arabic default RTL · BRD fields + identification + history
    shown from API · PATCH preferences · create sends no actor/owner/tenant
PERTURBATIONS (one per closed criterion)
  P-AC01 pets.get ignores tenant            -> test_ac01_profile_is_unreadable_from_another_tenant FAILS  ARMED
  P-AC02 history omits prescriptions        -> test_ac02_history_shows_lab_result_and_prescription FAILS  ARMED
  P-AC03 identification type unchecked      -> test_ac03_identification_is_never_free_text FAILS          ARMED
```

## Binding and evidence
- bindings FR-02: implements (9 symbols), routes (6), tests (3 files), fitness storage FILE → PERSISTENT_POSTGRES,
  audit_actor_source SESSION, ui_surface [petcare_web:/owner/pets]; stale `unresolved` cleared (U1 finding).
- evidence.json: AC-FR-02-01 {SERVED_APP_E2E, PERSISTENCE, TENANT_ISOLATION: TEST · UI: ARTEFACT} ·
  AC-FR-02-02 {SERVED_APP_E2E: TEST · UI, ARABIC_RTL: ARTEFACT} · AC-FR-02-03 {PERSISTENCE: TEST}.
  ARTEFACT = `petcare_web/__tests__/pet-profile.test.tsx` with sha256.
- served_routes.json regenerated: 33 → 38 routes.

## Step 6 — first real ACCEPTED
`tests/governance/test_ratified_pack.py::test_ratification_is_not_acceptance` failed as predicted
(MVC-ACCEPT-CHECK-001 F-2). Its body was replaced with: every ACCEPTED FR has acceptance_state ACCEPTED.
No other change to that file.

## Results
```
REGRESSION_LOCAL=995 passed / 0 skipped · WEB_UNIT(local)=131 passed · TSC clean
CHECKER: FR-02 ACCEPTED; no FR engineering status lowered (M4)
SCANNERS: SECRET_SCAN=CLEAN · ACTIVE_LITERAL_DEFAULT=0 · BUNDLES FAILED=0 · DIFF_CHECK=CLEAN
CI config: verify.yml PostgreSQL-controls list += test_pet_profile_postgres.py (required by test_ci_postgres_coverage)
```

## Findings
- **UI/ARABIC_RTL evidence is ARTEFACT-kind.** The checker binds TEST evidence only to pytest nodes; the vitest file
  is registered by sha256 and EXECUTED by CI's web-unit step, not by the checker. Known limit, recorded.
- `uphr_service` remains instantiated in main.py but no longer backs the pet profile; FR-21's UPHR binding is untouched.
